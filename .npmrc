# Suppress optional dependencies warnings
optional=true

# Reduce npm output verbosity
loglevel=warn

# Use package-lock.json
package-lock=true

# Use legacy peer deps to handle React 19 compatibility
legacy-peer-deps=true
